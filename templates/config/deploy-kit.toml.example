# Deploy-kit configuration example
# Copy this to your project root as deploy-kit.toml

[deploy]
# Project name (required if no pyproject.toml)
# For Python projects, this is auto-detected from pyproject.toml [project] name
name = "my-project"

# Project version (optional, defaults to "0.0.0")
# For Python projects, this is auto-detected from pyproject.toml [project] version
# version = "1.0.0"

# Override default port (default: 8000)
port = 8001

# Custom healthcheck path (default: /)
healthcheck_path = "/health"

# Number of old tarballs to keep (default: 3)
keep_tarballs = 5

# Target architecture for Docker build (default: auto-detected from build system)
# Options: "linux/arm64" or "linux/amd64"
# Can also be set via DEPLOY_ARCH environment variable
# architecture = "linux/amd64"

# Deployment target configuration
# Note: CLI arguments and environment variables take precedence over these settings

# SSH target for Compose backend (alternatively use DEPLOY_TARGET env var or CLI arg)
# Example: "user@host.example.com" or "user@192.168.1.100"
# ssh_target = "deploy@production.example.com"

# Portainer URL for API backend (alternatively use PORTAINER_URL env var or CLI arg)
# Example: "https://portainer.example.com" or "http://localhost:9000"
# portainer_url = "https://portainer.example.com"

# Security note: PORTAINER_API_KEY should ALWAYS be set as environment variable
# Recommended: Store in .env.sops (encrypted with SOPS) - deploy-kit decrypts automatically
# Never commit API keys to deploy-kit.toml or plain .env files
